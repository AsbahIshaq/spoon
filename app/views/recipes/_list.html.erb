<% recipes.each do |recipe| %>
  <%= link_to(recipe_path(recipe.id)) do %>
    <div class="contentful-product">
      <div class="contentful-title">
        <h1><%= recipe.title %></h1>
      </div>
      <img src="<%= recipe.photo.url %>" width=600 height=400 />
    </div>
  <% end %>
<% end %>